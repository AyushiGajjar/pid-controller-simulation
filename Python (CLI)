cd python
python -m venv .venv
# Windows: .venv\Scripts\activate
# Linux/Mac: source .venv/bin/activate
pip install -r requirements.txt

# Example: first-order plant, τ=1.0, PID = (1.5, 0.5, 0.05)
python pid_simulation.py --plant first --tau 1.0 --kp 1.5 --ki 0.5 --kd 0.05 --tend 10
